---
phase: 6
plan: "06-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/bookings/[id]/complete/route.ts
  - src/lib/crm/clients.ts
  - src/lib/crm/campaigns.ts
  - src/emails/PostTripReviewRequestEmail.tsx
  - src/lib/resend.ts
  - src/app/admin/(protected)/bookings/[id]/page.tsx
  - .env.example
autonomous: false
requirements: [CRM-01, CRM-02, CRM-03, CRM-04, CRM-05]
must_haves:
  truths:
    - "Booking completion is the trigger that enriches or creates the client record."
    - "The client profile is updated from the completed booking and lead data that already exists in Neon."
    - "A post-trip review request can be sent immediately from the same transactional email layer the repo already uses."
  artifacts:
    - path: "src/lib/crm/clients.ts"
      provides: "Client sync and enrichment from completed bookings"
    - path: "src/lib/crm/campaigns.ts"
      provides: "Anniversary, seasonal, and re-engagement reminder scheduling"
    - path: "src/app/api/admin/bookings/[id]/complete/route.ts"
      provides: "Admin-controlled booking completion and CRM trigger"
  key_links:
    - from: "src/app/api/admin/bookings/[id]/complete/route.ts"
      to: "src/lib/crm/clients.ts"
      via: "completion trigger to client sync"
      pattern: "completed|clientId|booking|lead"
    - from: "src/lib/crm/clients.ts"
      to: "src/lib/db/schema/clients.ts"
      via: "CRM enrichment"
      pattern: "preferredSpecies|totalTrips|totalSpend|lastTripDate"
---

# Plan 06-01: Client Sync, Completion Trigger, and CRM Reminder Scheduling

## Goal

Create the client lifecycle trigger for completed bookings, send the post-trip review request, and schedule the downstream CRM reminder cadence.

<execution_context>
@C:/Users/angel/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/angel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/GAFF/.planning/PROJECT.md
@d:/GAFF/.planning/ROADMAP.md
@d:/GAFF/.planning/STATE.md
@d:/GAFF/.planning/phases/06-crm-reviews-agent/06-CONTEXT.md
@d:/GAFF/.planning/phases/06-crm-reviews-agent/06-RESEARCH.md
@d:/GAFF/src/lib/db/schema/bookings.ts
@d:/GAFF/src/lib/db/schema/clients.ts
@d:/GAFF/src/lib/resend.ts
@d:/GAFF/src/emails/BookingConfirmationEmail.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the booking completion trigger and client sync helper</name>
  <read_first>
    - d:/GAFF/.planning/phases/06-crm-reviews-agent/06-CONTEXT.md
    - d:/GAFF/.planning/phases/06-crm-reviews-agent/06-RESEARCH.md
    - d:/GAFF/src/lib/db/schema/bookings.ts
    - d:/GAFF/src/lib/db/schema/clients.ts
  </read_first>
  <files>src/app/api/admin/bookings/[id]/complete/route.ts, src/lib/crm/clients.ts, src/app/admin/(protected)/bookings/[id]/page.tsx</files>
  <action>
Create a secure admin-only booking completion endpoint that marks a booking complete and syncs the related client record from the existing lead/booking data.

The client sync helper should upsert the client profile, update repeat-guest aggregates, and preserve the booking-to-client linkage so later CRM features can reuse the same record.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'complete|clientId|completed|sync' d:/GAFF/src/app/api/admin/bookings/[id]/complete/route.ts d:/GAFF/src/lib/crm/clients.ts` returns the completion trigger and client sync helper
    - `rg 'clientId|completed|Back to bookings|Booking detail' d:/GAFF/src/app/admin/(protected)/bookings/[id]/page.tsx` returns the booking detail surface the trigger extends
    - `npm run type-check` exits with code 0
  </acceptance_criteria>
  <done>Completed bookings can create or enrich client records safely from the admin side.</done>
</task>

<task type="auto">
  <name>Task 2: Add CRM reminder scheduling and post-trip review email</name>
  <read_first>
    - d:/GAFF/.planning/phases/06-crm-reviews-agent/06-CONTEXT.md
    - d:/GAFF/.planning/phases/06-crm-reviews-agent/06-RESEARCH.md
    - d:/GAFF/src/lib/resend.ts
    - d:/GAFF/src/emails/BookingConfirmationEmail.tsx
  </read_first>
  <files>src/lib/crm/campaigns.ts, src/emails/PostTripReviewRequestEmail.tsx, src/lib/resend.ts, .env.example</files>
  <action>
Build the CRM scheduling helper that stores anniversary, seasonal, and re-engagement reminders in a durable queue-like layer.

Add a post-trip review request email template and send it after the booking is completed so the client lifecycle has an immediate follow-up touchpoint.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'review request|anniversary|seasonal|re-engagement|campaign' d:/GAFF/src/lib/crm/campaigns.ts d:/GAFF/src/emails/PostTripReviewRequestEmail.tsx` returns the CRM reminder flow
    - `rg 'PostTripReviewRequestEmail|send.*Email' d:/GAFF/src/lib/resend.ts d:/GAFF/src/emails/PostTripReviewRequestEmail.tsx` returns the transactional email path
    - `npm run lint` exits with code 0
  </acceptance_criteria>
  <done>The CRM phase can notify clients immediately and schedule the longer lifecycle reminders.</done>
</task>

