---
phase: 5
plan: "05-02"
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - .env.example
  - src/app/api/channels/openclaw/route.ts
  - src/lib/chat/openclaw.ts
  - src/lib/chat/inbound.ts
autonomous: false
requirements: [CHAT-03, CHAT-04, LEAD-01, LEAD-05]
must_haves:
  truths:
    - "OpenClaw hooks are authenticated and normalized server-side before any lead data is written."
    - "WhatsApp inbound messages are converted into the same lead source of truth as the web chat path."
    - "Lead activity history records the channel and action so later admin review can reconstruct the conversation."
  artifacts:
    - path: "src/app/api/channels/openclaw/route.ts"
      provides: "Authenticated OpenClaw webhook receiver"
    - path: "src/lib/chat/openclaw.ts"
      provides: "OpenClaw verification and payload normalization helpers"
    - path: "src/lib/chat/inbound.ts"
      provides: "Shared inbound message normalization and lead write helpers"
  key_links:
    - from: "src/app/api/channels/openclaw/route.ts"
      to: "src/lib/chat/inbound.ts"
      via: "signed WhatsApp event to normalized lead write"
      pattern: "webhook|lead|normalize|activity"
    - from: "src/lib/chat/openclaw.ts"
      to: "src/lib/db/schema/leads.ts"
      via: "channel lead persistence"
      pattern: "source|whatsapp|metadata|status"
---

# Plan 05-02: OpenClaw Ingress and Cross-Channel Lead Writes

## Goal

Receive authenticated WhatsApp events from OpenClaw, normalize them into the lead model, and write the same lead/activity truth that the web chat flow uses.

<execution_context>
@C:/Users/angel/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/angel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/GAFF/.planning/PROJECT.md
@d:/GAFF/.planning/ROADMAP.md
@d:/GAFF/.planning/STATE.md
@d:/GAFF/.planning/phases/05-chat-lead-agent/05-CONTEXT.md
@d:/GAFF/.planning/phases/05-chat-lead-agent/05-RESEARCH.md
@d:/GAFF/.env.example
@d:/GAFF/src/lib/db/schema/leads.ts
@d:/GAFF/src/lib/db/schema/activities.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build authenticated OpenClaw webhook handling</name>
  <read_first>
    - d:/GAFF/.planning/phases/05-chat-lead-agent/05-CONTEXT.md
    - d:/GAFF/.planning/phases/05-chat-lead-agent/05-RESEARCH.md
    - d:/GAFF/.env.example
  </read_first>
  <files>src/app/api/channels/openclaw/route.ts, src/lib/chat/openclaw.ts, .env.example</files>
  <action>
Add a server route that accepts OpenClaw webhook events only when they carry the expected secret or bearer token, then normalize the payload into a small shared shape that the rest of the app can consume.

The route should reject unauthenticated calls, preserve enough metadata for debugging, and avoid leaking raw hook payloads to the client.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'openclaw|webhook|token|bearer|secret' d:/GAFF/src/app/api/channels/openclaw/route.ts d:/GAFF/src/lib/chat/openclaw.ts` returns authenticated hook handling
    - `rg 'OPENCLAW_URL|OPENCLAW_' d:/GAFF/.env.example` returns the channel configuration placeholders
    - `npm run type-check` exits with code 0
  </acceptance_criteria>
  <done>OpenClaw events can be received securely and normalized before any business logic runs.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize inbound chat into the lead pipeline</name>
  <read_first>
    - d:/GAFF/.planning/phases/05-chat-lead-agent/05-CONTEXT.md
    - d:/GAFF/.planning/phases/05-chat-lead-agent/05-RESEARCH.md
    - d:/GAFF/src/lib/db/schema/leads.ts
    - d:/GAFF/src/lib/db/schema/activities.ts
  </read_first>
  <files>src/lib/chat/inbound.ts, src/lib/chat/openclaw.ts, src/lib/db/schema/leads.ts, src/lib/db/schema/activities.ts</files>
  <action>
Write the shared inbound normalization layer that turns chat or WhatsApp messages into lead records, lead activities, and consistent source metadata.

The helper should be reusable by the future web chat pipeline and by OpenClaw so both channels create the same business record shape.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'normalize|lead|activity|source' d:/GAFF/src/lib/chat/inbound.ts d:/GAFF/src/lib/chat/openclaw.ts` returns shared inbound normalization helpers
    - `rg 'lead_activities|leadActivities|classification|metadata' d:/GAFF/src/lib/db/schema/leads.ts d:/GAFF/src/lib/db/schema/activities.ts` returns the lead model fields Phase 5 uses
    - `npm run lint` exits with code 0
  </acceptance_criteria>
  <done>The app can ingest chat-originated leads from multiple channels into one canonical database flow.</done>
</task>

