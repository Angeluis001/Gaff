---
phase: 2
plan: "02-03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/layout.tsx
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/app/privacy-policy/page.tsx
  - src/app/terms-of-service/page.tsx
  - public/og-image.jpg
autonomous: true
requirements: [LAND-10, SEO-01, SEO-02, SEO-03, SEO-04]
must_haves:
  truths:
    - "The landing app exposes production-grade metadata, JSON-LD, sitemap, robots rules, OG image, and canonical or alternate language data."
    - "Footer legal links resolve to real pages instead of 404s."
    - "The metadata and legal surface are complete before analytics and performance hardening begin."
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Final Metadata API configuration and JSON-LD injection."
    - path: "src/app/sitemap.ts"
      provides: "Phase 2 sitemap entries for the landing and legal pages."
    - path: "src/app/robots.ts"
      provides: "Crawler rules and sitemap reference for the public site."
    - path: "src/app/privacy-policy/page.tsx"
      provides: "Working privacy policy route linked from the footer."
    - path: "src/app/terms-of-service/page.tsx"
      provides: "Working terms route linked from the footer."
  key_links:
    - from: "src/components/landing/Footer.tsx"
      to: "/privacy-policy"
      via: "footer legal href"
      pattern: "href=\"/privacy-policy\""
    - from: "src/components/landing/Footer.tsx"
      to: "/terms-of-service"
      via: "footer legal href"
      pattern: "href=\"/terms-of-service\""
    - from: "src/app/layout.tsx"
      to: "src/app/sitemap.ts"
      via: "shared canonical public URL assumptions"
      pattern: "metadataBase|sitemap"
---

# Plan 02-03: SEO Metadata, JSON-LD, Sitemap, Robots, and Legal Pages

## Goal

Finish the landing page's crawlable and legally complete surface before analytics and performance hardening. This plan isolates the SEO and legal work so execution stays below the plan-size threshold while still completing the full metadata contract for Phase 2.

<execution_context>
@C:/Users/angel/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/angel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/GAFF/.planning/PROJECT.md
@d:/GAFF/.planning/ROADMAP.md
@d:/GAFF/.planning/STATE.md
@d:/GAFF/.planning/phases/02-landing-page/02-CONTEXT.md
@d:/GAFF/.planning/phases/02-landing-page/02-RESEARCH.md
@d:/GAFF/.planning/phases/02-landing-page/02-02-PLAN.md
@d:/GAFF/src/app/layout.tsx
@d:/GAFF/src/lib/constants.ts
@d:/GAFF/src/lib/translations.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search engine crawlers -> metadata routes | Robots, sitemap, canonical URLs, and JSON-LD must reflect the real public domain and avoid indexing admin or API paths. |
| Footer legal links -> route tree | Privacy and terms links must resolve to real pages instead of placeholders or 404s. |

## STRIDE Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | SEO metadata and legal links | mitigate | Define metadata in App Router files and create real legal routes so footer links cannot drift from implementation. |
| T-02-03-02 | Information Disclosure | Structured data | mitigate | Emit only public business information in JSON-LD; do not include secrets, private emails, or non-public coordinates. |
| T-02-03-03 | Spoofing | Social sharing surface | mitigate | Keep OG metadata and OG image aligned with the actual GAFF brand and landing content. |
</threat_model>

## Tasks

<task type="auto">
  <name>Task 1: Finish metadata and JSON-LD in the root layout</name>
  <read_first>
    - d:/GAFF/.planning/phases/02-landing-page/02-CONTEXT.md
    - d:/GAFF/.planning/phases/02-landing-page/02-RESEARCH.md
    - d:/GAFF/src/app/layout.tsx
    - d:/GAFF/src/lib/constants.ts
    - d:/GAFF/src/lib/translations.ts
  </read_first>
  <files>src/app/layout.tsx, public/og-image.jpg</files>
  <action>
Upgrade `src/app/layout.tsx` so it satisfies SEO-01, SEO-02, SEO-04, and the sharing portion of LAND-10:

- Set full Metadata API values: `metadataBase`, title template, description, keywords, canonical URL, alternate language mapping for the Spanish toggle strategy, Open Graph, Twitter card, and robots directives.
- Inject structured data for both `TouristAttraction` and `LocalBusiness` using project constants.
- Add or source a stable `public/og-image.jpg` placeholder aligned with the premium GAFF brand direction.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'metadataBase|openGraph|twitter|alternates|robots' d:/GAFF/src/app/layout.tsx` returns matches
    - `rg 'TouristAttraction|LocalBusiness|application/ld\+json' d:/GAFF/src/app/layout.tsx` returns matches
    - `Test-Path 'd:/GAFF/public/og-image.jpg'` returns `True`
    - `npm run type-check` exits with code 0
  </acceptance_criteria>
  <done>The landing app exposes a complete metadata and structured-data surface for search engines and social cards.</done>
</task>

<task type="auto">
  <name>Task 2: Add sitemap, robots, and legal pages</name>
  <read_first>
    - d:/GAFF/.planning/phases/02-landing-page/02-CONTEXT.md
    - d:/GAFF/src/app/layout.tsx
    - d:/GAFF/src/components/landing/Footer.tsx
  </read_first>
  <files>src/app/robots.ts, src/app/sitemap.ts, src/app/privacy-policy/page.tsx, src/app/terms-of-service/page.tsx</files>
  <action>
Create the crawl and legal-route surface for the landing page:

- Add `src/app/sitemap.ts` for the landing and legal pages.
- Add `src/app/robots.ts` that references the sitemap and excludes `/admin` and `/api`.
- Create `src/app/privacy-policy/page.tsx` and `src/app/terms-of-service/page.tsx` so the footer legal links resolve cleanly at `/privacy-policy` and `/terms-of-service`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'sitemap' d:/GAFF/src/app/robots.ts` returns a match
    - `rg 'privacy|terms' d:/GAFF/src/app/sitemap.ts` returns matches
    - `Test-Path 'd:/GAFF/src/app/privacy-policy/page.tsx'; Test-Path 'd:/GAFF/src/app/terms-of-service/page.tsx'` returns `True` for both files
    - `npm run type-check` exits with code 0
  </acceptance_criteria>
  <done>The footer legal links resolve to real content and the site exposes the required crawl metadata.</done>
</task>

<verification>
Run after Task 2:

```powershell
& npm.cmd run type-check
rg "metadataBase|TouristAttraction|LocalBusiness" d:/GAFF/src/app/layout.tsx
rg "sitemap|privacy|terms" d:/GAFF/src/app/robots.ts d:/GAFF/src/app/sitemap.ts
```

Manual verification required:
- Open a production-like URL and confirm the rendered page source contains the expected title, description, canonical, alternates, JSON-LD, and OG metadata.
- Confirm `/sitemap.xml` and `/robots.txt` render correctly on the production-like deployment.
- Confirm the footer legal links navigate to `/privacy-policy` and `/terms-of-service` on the production-like deployment.
```
</verification>

<success_criteria>
- Metadata, JSON-LD, sitemap, robots, OG image, and legal pages are all implemented and wired.
- Footer legal links resolve to real pages instead of 404s.
- The SEO and legal surface is verified on a production-like rendered output, not only in source files.
- Type-check remains clean after the SEO and legal pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-03-SUMMARY.md`.
</output>
