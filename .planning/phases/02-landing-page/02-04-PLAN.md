---
phase: 2
plan: "02-04"
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - .env.example
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/AnalyticsScripts.tsx
  - src/components/BotpressWidgetBridge.tsx
  - src/components/landing/FAQSection.tsx
  - src/lib/analytics.ts
  - package.json
autonomous: false
requirements: [LAND-06, PERF-01, PERF-02, PERF-03, INTG-06]
must_haves:
  truths:
    - "GA4, Meta Pixel, TikTok Pixel, and the Botpress web-chat shell load only when their public env vars are present, and the FAQ CTA can open the widget."
    - "The existing `gaff:booking-started` signal is explicitly bridged into GA4, Meta Pixel, and TikTok Pixel firing."
    - "Heavy landing sections are code-split or otherwise optimized so the production bundle stays within the Phase 2 performance budget."
    - "The phase does not complete unless Lighthouse, Core Web Vitals, and bundle-size thresholds are actually met and recorded."
  artifacts:
    - path: "src/components/AnalyticsScripts.tsx"
      provides: "Env-guarded GA4, Meta Pixel, and TikTok Pixel loading with afterInteractive behavior plus an explicit booking_started event listener."
    - path: "src/components/BotpressWidgetBridge.tsx"
      provides: "Env-guarded Botpress widget mount and listener for the stable chat-open event defined in Plan 02-02."
    - path: "src/lib/analytics.ts"
      provides: "Client helpers for pageview and booking_started event dispatch, plus explicit Phase 3 TODOs for deferred events."
    - path: "src/app/page.tsx"
      provides: "Dynamic imports or other below-fold loading optimizations for the landing route."
  key_links:
    - from: "src/components/AnalyticsScripts.tsx"
      to: "gaff:booking-started"
      via: "window event listener that calls trackBookingStarted and pixel helpers"
      pattern: "gaff:booking-started"
    - from: "src/components/landing/FAQSection.tsx"
      to: "src/components/BotpressWidgetBridge.tsx"
      via: "stable gaff:open-chat event and widget listener"
      pattern: "gaff:open-chat|botpress"
    - from: "src/app/layout.tsx"
      to: "src/components/AnalyticsScripts.tsx"
      via: "root-level analytics mounting"
      pattern: "AnalyticsScripts"
---

# Plan 02-04: Analytics, Botpress Launcher, and Performance Hardening

## Goal

Finish the landing page's measurement and hardening work after the visual, SEO, and legal surface is complete. This plan mounts analytics, connects the FAQ CTA to the Botpress widget shell, explicitly bridges booking-start events into analytics and pixels, and performs the blocking performance pass required to close Phase 2.

<execution_context>
@C:/Users/angel/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/angel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/GAFF/.planning/PROJECT.md
@d:/GAFF/.planning/ROADMAP.md
@d:/GAFF/.planning/STATE.md
@d:/GAFF/.planning/phases/02-landing-page/02-CONTEXT.md
@d:/GAFF/.planning/phases/02-landing-page/02-RESEARCH.md
@d:/GAFF/.planning/phases/02-landing-page/02-02-PLAN.md
@d:/GAFF/.planning/phases/02-landing-page/02-03-PLAN.md
@d:/GAFF/next.config.ts
@d:/GAFF/src/app/layout.tsx
@d:/GAFF/src/app/page.tsx
@d:/GAFF/src/components/landing/FAQSection.tsx
@d:/GAFF/src/components/landing/AvailabilityCalendarSection.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public analytics and chat IDs -> browser | Measurement IDs and Botpress public config are exposed to the client, but secrets and access tokens must remain server-side. |
| Production performance verification -> deployment platform | Final performance claims depend on a production-like build or deployed preview, not the dev server. |

## STRIDE Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | Pixel and Botpress setup | mitigate | Only use `NEXT_PUBLIC_*` identifiers client-side and never embed private tokens in source. |
| T-02-04-02 | Denial of Service | Client bundle size | mitigate | Dynamically import heavy below-fold sections and keep analytics/chat scripts env-guarded and afterInteractive where appropriate. |
| T-02-04-03 | Repudiation | Analytics events | mitigate | Centralize event helpers in `src/lib/analytics.ts` and explicitly bridge `gaff:booking-started` in `AnalyticsScripts.tsx`. |
</threat_model>

## Tasks

<task type="auto">
  <name>Task 1: Install analytics scripts, bridge booking_started, and mount the Botpress widget bridge</name>
  <read_first>
    - d:/GAFF/.planning/phases/02-landing-page/02-RESEARCH.md
    - d:/GAFF/src/app/layout.tsx
    - d:/GAFF/src/components/landing/FAQSection.tsx
    - d:/GAFF/src/components/landing/AvailabilityCalendarSection.tsx
    - d:/GAFF/.env.example
  </read_first>
  <files>.env.example, src/app/layout.tsx, src/components/AnalyticsScripts.tsx, src/components/BotpressWidgetBridge.tsx, src/components/landing/FAQSection.tsx, src/lib/analytics.ts</files>
  <action>
Implement the Phase 2 scope of INTG-06 and the LAND-06 chat-open behavior without compromising performance:

- Create `src/components/AnalyticsScripts.tsx` to mount `GoogleAnalytics` from `@next/third-parties/google` and load Meta Pixel plus TikTok Pixel with `next/script` and `strategy="afterInteractive"`.
- In `src/components/AnalyticsScripts.tsx`, add an explicit listener for the existing `gaff:booking-started` event. That listener must call `trackBookingStarted` from `src/lib/analytics.ts` and fan the event out to GA4, Meta Pixel, and TikTok Pixel when those IDs are configured.
- Create `src/components/BotpressWidgetBridge.tsx` to lazily mount the Botpress web-chat shell when the required public config is present. The bridge must listen for the stable `gaff:open-chat` event defined in Plan 02-02 and open the widget when received.
- Guard every analytics/chat mount behind the corresponding `NEXT_PUBLIC_*` env var so local development works without IDs.
- Create `src/lib/analytics.ts` helpers for pageview and `booking_started`. `lead_captured` and `booking_completed` stay as explicit Phase 3 TODOs.
- Update `.env.example` so the repo documents exactly one public env variable per platform and the Botpress widget config it expects.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'GoogleAnalytics|afterInteractive|fbq|ttq|gaff:booking-started|trackBookingStarted' d:/GAFF/src/components/AnalyticsScripts.tsx` returns matches
    - `rg 'gaff:open-chat|botpress|open|window\.botpress|webchat' d:/GAFF/src/components/BotpressWidgetBridge.tsx` returns matches
    - `rg 'trackBookingStarted|trackPageView|booking_completed|lead_captured' d:/GAFF/src/lib/analytics.ts` returns matches or explicit Phase 3 TODOs for the deferred events
    - `rg 'AnalyticsScripts|BotpressWidgetBridge' d:/GAFF/src/app/layout.tsx` returns matches
    - `rg 'gaff:open-chat' d:/GAFF/src/components/landing/FAQSection.tsx` returns a match
    - `rg 'NEXT_PUBLIC_GA|NEXT_PUBLIC_META_PIXEL_ID|NEXT_PUBLIC_TIKTOK_PIXEL_ID|NEXT_PUBLIC_BOTPRESS' d:/GAFF/.env.example` returns matches
    - `npm run type-check` exits with code 0
  </acceptance_criteria>
  <done>The landing page can measure traffic and booking intent, and the FAQ chat CTA opens a real Botpress launcher path instead of a dead-end placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize bundle and verify Lighthouse and Core Web Vitals thresholds</name>
  <read_first>
    - d:/GAFF/.planning/phases/02-landing-page/02-RESEARCH.md
    - d:/GAFF/src/app/page.tsx
    - d:/GAFF/next.config.ts
    - d:/GAFF/package.json
  </read_first>
  <files>src/app/page.tsx, next.config.ts, package.json</files>
  <action>
Perform the blocking optimization pass described in the roadmap success criteria:

- Update `src/app/page.tsx` to lazy-load heavy below-fold sections using `next/dynamic` where it meaningfully reduces initial JavaScript.
- Review media and script loading so hero content remains the LCP focus while analytics and secondary sections defer appropriately.
- Keep or refine `next.config.ts` optimization settings based on what Phase 2 actually imports.
- Add any lightweight repo-native verification support needed to inspect the production bundle size, but do not introduce tooling that conflicts with Turbopack.

Then run the project verification loop:
1. `npm run lint`
2. `npm run type-check`
3. `npm run build`
4. Run Lighthouse against a production-like URL and record the final Performance score, LCP, CLS, and landing-route JavaScript size in the summary artifact.
5. Validate the landing route's interactivity metric using the current Core Web Vitals metric: INP < 200ms on the production-like URL, and record the measured value in the summary artifact.

This plan does not complete unless the thresholds are actually met and recorded. If the metrics fail, keep iterating inside this plan instead of closing it with a blocker note.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'dynamic\(' d:/GAFF/src/app/page.tsx` returns at least one match for a heavy below-fold section
    - `npm run lint` exits with code 0
    - `npm run type-check` exits with code 0
    - `npm run build` exits with code 0
    - The final Lighthouse report recorded in the summary shows Performance > 90, LCP < 2.5s, and CLS < 0.1
    - The production bundle evidence recorded in the summary shows JavaScript < 200KB gzipped for the landing route
    - The summary records INP < 200ms on the production-like URL for the landing route
  </acceptance_criteria>
  <done>The landing page is optimized, buildable, and accompanied by passing performance evidence rather than an unverified performance claim.</done>
</task>

<verification>
Run after Task 2:

```powershell
& npm.cmd run lint
& npm.cmd run type-check
& npm.cmd run build
rg "dynamic\(" d:/GAFF/src/app/page.tsx
rg "GoogleAnalytics|fbq|ttq|gaff:booking-started" d:/GAFF/src/components/AnalyticsScripts.tsx
rg "gaff:open-chat|botpress" d:/GAFF/src/components/BotpressWidgetBridge.tsx
```

Manual verification required:
- Run Lighthouse against the production-like URL and record the passing final numbers in `02-04-SUMMARY.md`.
- Verify GA4, Meta Pixel, and TikTok Pixel fire only when their IDs are configured.
- Verify `booking_started` fires in GA4/Meta/TikTok when an available landing date is clicked.
- Verify the FAQ `Chat with us` CTA opens the Botpress widget when the Botpress config is present.
- Verify the landing route records INP < 200ms on the production-like URL and note the value in `02-04-SUMMARY.md`.
```
</verification>

<success_criteria>
- Analytics scripts and the Botpress widget bridge are env-guarded, centralized, and connected to pageview plus booking-start behavior.
- The landing page build passes lint, type-check, and production build validation.
- Performance verification is documented with passing real evidence, including INP.
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-04-SUMMARY.md` with measured analytics and performance evidence.
</output>
